import React from 'react';

// RightPanel component for displaying content in the right panel of the layout
const RightPanel = () => {
  return (
    <aside className="col-md-3 border-start" style={{ height: '100vh' }}>
      <div className="p-3" style={{ height: '100%' }}>
        {/* Content for Right Panel */}
      </div>
    </aside>
  );
};

export default RightPanel;
