// Importing React library for creating components
import React from 'react';

// Importing toast notifications from react-toastify library
import { toast, ToastContainer } from 'react-toastify';

const LeftPanel = () => {
  return (
    <aside className="col-md-1">
      {/* Empty left panel */}
    </aside>
  );
};

// Exporting LeftPanel component as default
export default LeftPanel;
